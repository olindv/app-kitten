# Фаза 0 «Фундамент» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Пустое, но полностью настроенное Expo/React Native приложение «Мой Котёнок» запускается на Android-эмуляторе и устройстве; линт, тесты и структура src/ готовы к фазе 1.

**Architecture:** Expo (последний стабильный SDK, TypeScript) с Continuous Native Generation — папка `android/` генерируется `expo run:android` и не хранится в git. Вся будущая логика живет в `src/`, вход через `index.ts` → `src/app/App.tsx`.

**Tech Stack:** Expo + TypeScript, React Navigation (native-stack), Zustand, AsyncStorage, react-native-svg, Reanimated, Gesture Handler, expo-audio, Jest (jest-expo) + @testing-library/react-native, oxlint + Prettier.

**Reference:** спека `docs/specs/2026-07-03-kitten-game-design.md` (§8 стек, §9 архитектура).

## Global Constraints

- ОС разработки: Windows 11, PowerShell. Android SDK: `%LOCALAPPDATA%\Android\Sdk`, JDK 17, Node 22.
- Проект в `X:\projects\android-proj`, git уже инициализирован (ветка `main`), папка содержит `README.md`, `.gitignore`, `docs/`, `.claude/` — скаффолд НЕ должен их затирать.
- Приложение: name «Мой Котёнок», slug `my-kitten`, android package `com.dnavras.mykitten`, orientation `default` (портрет и альбом).
- Полностью офлайн: не добавлять никаких сетевых/аналитических зависимостей.
- Язык UI — русский.
- Каждая задача заканчивается коммитом; сообщение — conventional commits.

---

### Task 1: Скаффолд Expo-проекта в текущую папку

**Files:**

- Create: `package.json`, `app.json`, `tsconfig.json`, `index.ts`, `App.tsx`, `assets/*` (из шаблона)
- Modify: `.gitignore` (слить с шаблонным + добавить `/android`, `/ios`)

**Interfaces:**

- Produces: рабочий Expo-проект; `npm run start` поднимает Metro. Последующие задачи полагаются на скрипты `package.json` и `app.json`.

- [ ] **Step 1: Скаффолд во временную папку** (папка непуста — create-expo-app в неё напрямую откажется)

```powershell
npx create-expo-app@latest tmp-kitten --template blank-typescript --no-install
```

Expected: папка `tmp-kitten` с `App.tsx`, `app.json`, `package.json`, `tsconfig.json`, `index.ts`, `assets/`, `.gitignore`.

- [ ] **Step 2: Слить .gitignore и перенести файлы**

```powershell
Add-Content .gitignore "`n# --- from expo template ---"
Get-Content tmp-kitten\.gitignore | Add-Content .gitignore
Add-Content .gitignore "`n# CNG: native folders are generated by expo prebuild`n/android`n/ios"
Remove-Item tmp-kitten\.gitignore
Remove-Item tmp-kitten\README.md -ErrorAction SilentlyContinue
Get-ChildItem tmp-kitten -Force | Move-Item -Destination .
Remove-Item tmp-kitten
```

Expected: в корне лежат файлы шаблона; наши `README.md`, `docs/`, `.git/` не тронуты. Дубликаты строк в `.gitignore` допустимы.

- [ ] **Step 3: Установить зависимости**

```powershell
npm install
```

Expected: `node_modules/` создан, без ошибок (warnings допустимы).

- [ ] **Step 4: Настроить app.json**

Заменить блок `expo` в `app.json` (сохранив ссылки на assets из шаблона):

```json
{
  "expo": {
    "name": "Мой Котёнок",
    "slug": "my-kitten",
    "version": "0.1.0",
    "orientation": "default",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "splash": {
      "image": "./assets/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#FFF7E6"
    },
    "android": {
      "package": "com.dnavras.mykitten",
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#FFF7E6"
      }
    }
  }
}
```

Примечание: имена файлов в `assets/` могут отличаться по шаблону — использовать те, что реально лежат в папке.

- [ ] **Step 5: Проверить, что Metro стартует**

```powershell
npx expo start --port 8081
```

Expected: QR-код и меню Metro без ошибок. Остановить Ctrl+C.

- [ ] **Step 6: Commit**

```powershell
git add -A
git commit -m "chore: scaffold Expo blank-typescript app"
```

---

### Task 2: oxlint + Prettier

_Изменение по ходу выполнения: владелец проекта выбрал oxlint вместо ESLint._

**Files:**

- Create: `.oxlintrc.json`, `.prettierrc`
- Modify: `package.json` (скрипты, devDependencies)

**Interfaces:**

- Produces: `npm run lint` и `npm run format` — используются в конце каждой следующей задачи.

- [x] **Step 1: Установить oxlint и prettier**

```powershell
npm install --save-dev oxlint prettier
```

- [x] **Step 2: Конфиги**

`.oxlintrc.json`:

```json
{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["typescript", "react", "oxc"],
  "categories": {
    "correctness": "error",
    "suspicious": "warn"
  },
  "rules": {
    "react/react-in-jsx-scope": "off",
    "react/style-prop-object": "off"
  },
  "ignorePatterns": ["android/**", "ios/**", "dist/**", ".expo/**", "node_modules/**"]
}
```

Оба off-правила — ложные срабатывания для React Native: новый JSX transform не требует `React` в скоупе, а `style` в RN — объект из `StyleSheet.create`.

`.prettierrc`:

```json
{
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100
}
```

- [x] **Step 3: Скрипты в package.json**

В `"scripts"` добавить:

```json
"lint": "oxlint",
"format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
```

- [x] **Step 4: Прогнать и проверить**

```powershell
npm run format
npm run lint
```

Expected: format переписывает файлы, lint — 0 errors/warnings.

- [x] **Step 5: Commit**

```powershell
git add -A
git commit -m "chore: add oxlint and prettier"
```

---

### Task 3: Runtime-зависимости

**Files:**

- Modify: `package.json`, `package-lock.json`
- Create: `babel.config.js` (если шаблон не создал)

**Interfaces:**

- Produces: установленные и совместимые с SDK версии: `@react-navigation/native`, `@react-navigation/native-stack`, `react-native-screens`, `react-native-safe-area-context`, `react-native-gesture-handler`, `react-native-reanimated`, `react-native-svg`, `expo-audio`, `@react-native-async-storage/async-storage`, `zustand`.

- [ ] **Step 1: Установить через expo install** (подбирает версии под SDK)

```powershell
npx expo install @react-navigation/native @react-navigation/native-stack react-native-screens react-native-safe-area-context react-native-gesture-handler react-native-reanimated react-native-svg expo-audio @react-native-async-storage/async-storage zustand
```

Expected: все пакеты в `dependencies`, без peer-конфликтов.

- [ ] **Step 2: babel.config.js**

Если файла нет — создать:

```js
module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
  };
};
```

`babel-preset-expo` сам подключает плагин Reanimated/worklets для установленной версии — вручную плагины не добавлять.

- [ ] **Step 3: Проверка совместимости**

```powershell
npx expo-doctor
```

Expected: все проверки зеленые (предупреждение про кастомные поля допустимо).

- [ ] **Step 4: Commit**

```powershell
git add -A
git commit -m "chore: add navigation, state, svg, animation and audio deps"
```

---

### Task 4: Структура src/ и вход приложения

**Files:**

- Create: `src/app/App.tsx`, `src/screens/PlaceholderScreen.tsx`
- Modify: `index.ts`
- Delete: `App.tsx` (корневой, из шаблона)

**Interfaces:**

- Produces: `App` (named export из `src/app/App.tsx`) — корневой компонент с провайдерами `GestureHandlerRootView` → `SafeAreaProvider` → `NavigationContainer`; тип `RootStackParamList = { Game: undefined }`. Фаза 1 добавляет экраны в этот стек.

- [ ] **Step 1: Создать PlaceholderScreen**

`src/screens/PlaceholderScreen.tsx`:

```tsx
import { StyleSheet, Text, View } from 'react-native';

export function PlaceholderScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>🐱 Мой Котёнок</Text>
      <Text style={styles.subtitle}>Фаза 0 — фундамент готов</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#FFF7E6',
  },
  title: { fontSize: 36 },
  subtitle: { fontSize: 16, marginTop: 12, color: '#8A7B66' },
});
```

- [ ] **Step 2: Создать корневой App**

`src/app/App.tsx`:

```tsx
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { SafeAreaProvider } from 'react-native-safe-area-context';

import { PlaceholderScreen } from '../screens/PlaceholderScreen';

export type RootStackParamList = {
  Game: undefined;
};

const Stack = createNativeStackNavigator<RootStackParamList>();

export function App() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <SafeAreaProvider>
        <NavigationContainer>
          <Stack.Navigator screenOptions={{ headerShown: false }}>
            <Stack.Screen name="Game" component={PlaceholderScreen} />
          </Stack.Navigator>
        </NavigationContainer>
      </SafeAreaProvider>
    </GestureHandlerRootView>
  );
}
```

- [ ] **Step 3: Перенаправить вход и удалить шаблонный App.tsx**

`index.ts`:

```ts
import { registerRootComponent } from 'expo';

import { App } from './src/app/App';

registerRootComponent(App);
```

```powershell
Remove-Item App.tsx
```

- [ ] **Step 4: Проверить линт и Metro**

```powershell
npm run lint
npx expo start
```

Expected: lint 0 errors; Metro стартует (бандл соберется при подключении клиента — достаточно отсутствия ошибок конфигурации). Остановить Ctrl+C.

- [ ] **Step 5: Commit**

```powershell
git add -A
git commit -m "feat: app entry with navigation shell and placeholder screen"
```

---

### Task 5: Jest + смоук-тест

**Files:**

- Create: `src/app/__tests__/App.test.tsx`
- Modify: `package.json` (jest-конфиг, скрипт, dev-зависимости)

**Interfaces:**

- Consumes: `App` из `src/app/App.tsx` (Task 4).
- Produces: `npm test` — обязательный шаг всех задач фазы 1+.

- [ ] **Step 1: Установить тестовые зависимости**

```powershell
npx expo install jest jest-expo --dev
npm install --save-dev @testing-library/react-native @types/jest
```

- [ ] **Step 2: Конфиг Jest в package.json**

Добавить в `package.json`:

```json
"jest": {
  "preset": "jest-expo",
  "setupFiles": ["react-native-gesture-handler/jestSetup"],
  "transformIgnorePatterns": [
    "node_modules/(?!((jest-)?react-native|@react-native(-community)?|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|expo-modules-core|react-native-svg|react-native-reanimated|react-native-gesture-handler|react-native-screens|react-native-safe-area-context|@react-native-async-storage/.*)/)"
  ]
},
```

и в `"scripts"`: `"test": "jest"`.

- [ ] **Step 3: Написать падающий смоук-тест**

`src/app/__tests__/App.test.tsx`:

```tsx
import { render } from '@testing-library/react-native';

import { App } from '../App';

test('renders home placeholder', () => {
  const { getByText } = render(<App />);
  expect(getByText('🐱 Мой Котёнок')).toBeTruthy();
});
```

Сначала убедиться, что тест вообще исполняется: временно поменять ожидание на `expect(getByText('нет такого текста')).toBeTruthy();`

```powershell
npm test
```

Expected: FAIL — «Unable to find an element with text: нет такого текста». Это доказывает, что тест не проходит вхолостую.

- [ ] **Step 4: Вернуть правильное ожидание, тест зеленый**

Вернуть `getByText('🐱 Мой Котёнок')`.

```powershell
npm test
```

Expected: PASS, 1 test.

- [ ] **Step 5: Commit**

```powershell
git add -A
git commit -m "test: add jest-expo setup with app smoke test"
```

---

### Task 6: Нативная сборка и запуск на эмуляторе

**Files:**

- Create: `android/` (генерируется, в git не попадает — см. `.gitignore`)

**Interfaces:**

- Consumes: всё предыдущее.
- Produces: подтверждение, что весь стек (Reanimated, SVG, навигация) собирается нативно. Далее фаза 1 разрабатывается против этой сборки.

- [ ] **Step 1: Запустить/проверить эмулятор**

```powershell
& "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe" -list-avds
```

Expected: минимум один AVD. Если список пуст — создать AVD в Android Studio (Device Manager → Pixel 7, API 34+) и повторить. Запустить выбранный:

```powershell
Start-Process "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe" -ArgumentList "-avd", "<ИМЯ_AVD>"
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" wait-for-device
```

- [ ] **Step 2: Нативная сборка**

```powershell
npx expo run:android
```

Expected: prebuild генерирует `android/`, Gradle собирает (первый раз 5–15 мин), `BUILD SUCCESSFUL`, приложение открывается на эмуляторе и показывает «🐱 Мой Котёнок / Фаза 0 — фундамент готов».

- [ ] **Step 3: Проверить, что android/ не попал в git**

```powershell
git status --short
```

Expected: `android/` отсутствует в выводе (заигнорен). Если появился — проверить `.gitignore` строку `/android`.

- [ ] **Step 4: Финальная проверка фазы**

```powershell
npm run lint
npm test
```

Expected: lint 0 errors, tests PASS.

- [ ] **Step 5: Commit**

```powershell
git add -A
git commit -m "chore: verify native android build (CNG, android/ git-ignored)"
```

Примечание: если нечего коммитить (сборка не изменила отслеживаемые файлы) — пропустить.

---

### Task 7: GitHub remote и push

**Files:** нет (только git-конфигурация).

**Interfaces:**

- Produces: `origin` → GitHub, ветка `main` запушена. Все дальнейшие фазы пушатся сюда.

- [ ] **Step 1: Создать репозиторий и push**

Вариант A — установлен и авторизован gh CLI:

```powershell
gh repo create my-kitten --private --source . --push
```

Вариант B — репозиторий создан вручную на github.com (private, без README/лицензии):

```powershell
git remote add origin https://github.com/<username>/my-kitten.git
git push -u origin main
```

Expected: push успешен, на GitHub видны `docs/`, `src/`, конфиги.

- [ ] **Step 2: Проверка**

```powershell
git remote -v
git log --oneline -3
```

Expected: origin указывает на GitHub; локальный HEAD = origin/main.

---

## Definition of Done (фаза 0)

- `npx expo run:android` ставит и открывает приложение на эмуляторе (и устройстве при подключении).
- `npm run lint` — 0 ошибок; `npm test` — зеленый смоук-тест.
- Структура `src/app`, `src/screens` создана; вход через `index.ts` → `src/app/App.tsx`.
- `android/` не в git; репозиторий на GitHub.

План фазы 1 («Котёнок и дом», MVP) пишется отдельным документом после завершения фазы 0 — он опирается на фактические версии пакетов из `package.json`.
